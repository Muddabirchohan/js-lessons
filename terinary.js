const renderRole = (role) => {

    if (role === "admin") {
        console.log("admin user")
    } else if (role === "subAdmin") {
        console.log("sub admin")
    } else {
        console.log("default")
    }

}

// role === "admin" ? console.log("admin user") : role === "subAdmin" ? console.log("sub admin") : console.log("default")


role === "admin" ? 

console.log("inside if block") 

: role === "subAdmin" ? 

console.log("inside else if") 

: console.log("else")


